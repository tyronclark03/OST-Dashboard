# =========================================
# OST Dashboard - Scanner Configuration
# =========================================

# File Size Thresholds (in GB)
warning_threshold=20
critical_threshold=30

# Directory Paths
mock_data_dir=tests/mock_data
logs_dir=logs

# Display Settings
show_normal=false

# === Action Behavior ===
action_on_critical=move
action_on_warning=log_only
archive_dir=archive
dry_run=true
